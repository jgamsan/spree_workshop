<%= form_for(@spree_workshop) do |f| %>
  <% if @spree_workshop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spree_workshop.errors.count, "error") %> prohibited this spree_workshop from being saved:</h2>

      <ul>
      <% @spree_workshop.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
